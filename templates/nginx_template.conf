## Auto generated by quick-vhost.sh ( https://github.com/itslenny/quick-vhost )
## nginx virtual host config file for: %VHOST%

#proxy requests to apache
server {
    listen   80;
    server_name %VHOST%;
    location / {
        proxy_set_header X-Real-IP  $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header Host $host;
        proxy_pass http://127.0.0.1:8080;
    }
}
#create static subdomain served directly by nginx
server {
    listen   80;
    root %DOCROOT%/public;
    index index.html index.htm;
    server_name static.%VHOST%;
}